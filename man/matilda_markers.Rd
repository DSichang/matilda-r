% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{matilda_markers}
\alias{matilda_markers}
\title{Per-cell-type feature importance (markers) via integrated gradients / saliency.}
\usage{
matilda_markers(
  x,
  reference = NULL,
  label = NULL,
  method = c("IntegratedGradient", "Saliency"),
  assay = "counts",
  adt_exp = "ADT",
  atac_exp = "ATAC",
  device = c("auto", "cpu", "cuda")
)
}
\arguments{
\item{x}{SCE/MAE (with a model, or a query) / matrix list.}

\item{reference}{a trained object/model to use; \code{NULL} = use \code{x}'s own.}

\item{label}{optional cell-type labels for the ground-truth column of the report.}

\item{method}{"IntegratedGradient" (default) or "Saliency".}

\item{assay, adt_exp, atac_exp}{assay/altExp selectors.}

\item{device}{"auto"/"cpu"/"cuda".}
}
\value{
data.frame(celltype, feature, importance).
}
\description{
Per-cell-type feature importance (markers) via integrated gradients / saliency.
}
\examples{
sce <- matilda_example_sce()
\donttest{
  sce <- matilda_train(sce, label = "cell_type", epochs = 2L)
  mk <- matilda_markers(sce)
}
}
